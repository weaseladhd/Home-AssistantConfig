---
name: Home Assistant CI

# yamllint disable-line rule:truthy
on:
  push:
  pull_request:
  schedule:
    - cron: 0 12 * * *

permissions:
  contents: read

jobs:
  secret-scan:
    name: Scan changed commits for secrets
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Run Gitleaks on changed commits
        env:
          EVENT_NAME: ${{ github.event_name }}
          BEFORE_SHA: ${{ github.event.before }}
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          SHA: ${{ github.sha }}
        run: |
          if [ "$EVENT_NAME" = "pull_request" ]; then
            range="$BASE_SHA..$HEAD_SHA"
          elif [ "$BEFORE_SHA" != "0000000000000000000000000000000000000000" ]; then
            range="$BEFORE_SHA..$SHA"
          else
            range="$SHA^..$SHA"
          fi
          docker run --rm \
            -v "$PWD:/repo" \
            zricethezav/gitleaks:v8.28.0 \
            git --redact --log-opts="$range" /repo

  yamllint:
    name: 🧹 yamllint
    runs-on: ubuntu-24.04
    steps:
      - name: ⤵️ Check out configuration from GitHub
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: 🚀 Run yamllint
        uses: frenck/action-yamllint@34b4bbcaeabedcfefad6adea8c5bbc42af0e2d47 # v1.5.0

  remarklint:
    runs-on: ubuntu-24.04
    steps:
      - name: ⤵️ Check out configuration from GitHub
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Running Remark lint
        uses: "docker://pipelinecomponents/remark-lint:latest"
        continue-on-error: true
        with:
          args: "remark --no-stdout --color --frail --use preset-lint-recommended ."

  home-assistant:
    name: "Home Assistant Core ${{ matrix.version }} Configuration Check"
    needs: [yamllint]
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        version: ["stable", "beta", "dev"]
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Create fake empty files
        run: |
          touch ./home-assistant_v2.db
          touch ./home-assistant.log
      - name: Create empty custom components folder
        run: |
          mkdir -p ./custom_components
      - name: Pin external custom components
        run: |
          clone_at() {
            git clone --no-checkout "$1" "$3"
            git -C "$3" checkout --detach "$2"
          }
          clone_at https://github.com/metbril/home-assistant-brandstofprijzen.git 654d9e1067b88b453cb9248a275b675a4ec0a040 home-assistant-brandstofprijzen
          clone_at https://github.com/briis/unifiprotect.git 7ba7cdf52826ea0232facfb89c0df1daf003a345 unifiprotect
          clone_at https://github.com/thomasloven/hass-browser_mod.git 48ee37e663e9a894141dab2901a2a564149a2623 hass-browser_mod
          clone_at https://github.com/fondberg/spotcast.git d54c8dcfffc4df31182ccfcd4201dc0e36d30d61 spotcast
          clone_at https://github.com/xirixiz/homeassistant-afvalwijzer.git f7df20183abf230e70821e95fd5b7adf86e700ad homeassistant-afvalwijzer
          clone_at https://github.com/PiotrMachowski/Home-Assistant-custom-components-Xiaomi-Cloud-Map-Extractor.git 62285a755a13a9e1178a8e76f75830bce320eea5 Home-Assistant-custom-components-Xiaomi-Cloud-Map-Extractor
          clone_at https://github.com/rgc99/irrigation_unlimited.git 05a5eaa7a7d8ad43bf3f903ce194ac7f34ce8 irrigation_unlimited
          clone_at https://github.com/bramstroker/homeassistant-powercalc.git 22b3365920db3027306e406e56ca20cb31d5547 homeassistant-powercalc
          clone_at https://github.com/custom-components/sensor.unifigateway.git b6bb00c6580bcf0045929334f37fffbeb1a27 sensor.unifigateway
          clone_at https://github.com/cytech/Home-Assistant-wundergroundpws.git d89e850bf171e65467b299a8193fd0de63c399f7 Home-Assistant-wundergroundpws
          clone_at https://github.com/delzear/hass-formulaoneapi.git 2167c51673392d309746480a209eed9e5d817b72 hass-formulaoneapi
          clone_at https://github.com/1RandomDev/homeassistant-gotify.git 0282865d9531fd8b9da6a9cd2bef35e055f30671 homeassistant-gotify
      - name: Clone custom component Brandstofprijzen
        run: |
          rm -rf ./custom_components/brandstofprijzen
          cp -rv home-assistant-brandstofprijzen/custom_components/. ./custom_components/
      - name: Clone custom component Unifi protect
        run: |
          rm -rf ./custom_components/unifiprotect
          cp -rv unifiprotect/custom_components/. ./custom_components/
      - name: Clone custom component Browser mod
        run: |
          rm -rf ./custom_components/browser_mod
          cp -rv hass-browser_mod/custom_components/. ./custom_components/
      - name: Clone custom component Spotcast
        run: |
          rm -rf ./custom_components/spotcast
          cp -rv spotcast/custom_components/. ./custom_components/
      - name: Clone custom component Afvalwijzer
        run: |
          rm -rf ./custom_components/afvalwijzer
          cp -rv homeassistant-afvalwijzer/custom_components/. ./custom_components/
      - name: Clone custom component Xiaomi map cloud extractor
        run: |
          rm -rf ./custom_components/xiaomi_cloud_map_extractor
          cp -rv Home-Assistant-custom-components-Xiaomi-Cloud-Map-Extractor/custom_components/. ./custom_components/
      - name: Clone custom component Irrigation Unlimited
        run: |
          rm -rf ./custom_components/irrigation_unlimited
          cp -rv irrigation_unlimited/custom_components/. ./custom_components/
      - name: Clone custom component Powercalc
        run: |
          rm -rf ./custom_components/powercalc
          cp -rv homeassistant-powercalc/custom_components/. ./custom_components/
      - name: Clone custom component unifigateway
        run: |
          rm -rf ./custom_components/unifigateway
          cp -rv sensor.unifigateway/custom_components/. ./custom_components/
      - name: Clone custom component wundergroundpws
        run: |
          rm -rf ./custom_components/wundergroundpws
          cp -rv Home-Assistant-wundergroundpws/custom_components/. ./custom_components/
      - name: Clone custom component formulaone_api
        run: |
          rm -rf ./custom_components/formulaone_api
          cp -rv hass-formulaoneapi/custom_components/. ./custom_components/
      - name: Clone custom component Gotify
        run: |
          rm -rf ./custom_components/gotify
          cp -rv homeassistant-gotify/custom_components/. ./custom_components/
      - name: Check configuration
        uses: frenck/action-home-assistant@941d5d917f4c1c7a7e7d4087526daf90d53f4437 # v1.4.1
        with:
          secrets: ./.stubs/ci_secrets.yaml
          version: "${{ matrix.version }}"
